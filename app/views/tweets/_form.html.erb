<%= simple_form_for(@tweet) do |f| %>
  
  <div class="field">
    <div class="control">
      <%= f.input :tweet, label: "Bullshit as you want!", input_html: { class: "textarea"}, wrapper: false, label_html: {class: "label"}, placeholder: "No rules here..." %>
    </div>
  </div>
  <%= f.button :submit, class: "button is-info" %>
<% end %>